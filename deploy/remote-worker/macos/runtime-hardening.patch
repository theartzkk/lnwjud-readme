--- a/dist/index.js
+++ b/dist/index.js
@@ -106,8 +106,10 @@
             // Now we can send regular logging messages
             transport.sendLog('info', 'Server connected successfully');
             transport.sendLog('info', 'MCP fully initialized, all startup messages sent');
-            // Preemptively check/download Chrome for PDF generation (runs in background)
-            ensureChromeAvailable();
+            // Remote workers stay network-light at startup; PDF generation resolves Chrome lazily.
+            if (process.env.DC_REMOTE_DEVICE !== 'true') {
+                ensureChromeAvailable();
+            }
         };
         await server.connect(transport);
     }
--- a/dist/remote-device/device.js
+++ b/dist/remote-device/device.js
@@ -17,6 +17,16 @@
 const SEEN_CALL_IDS_MAX = 100;
 const PERSISTED_DEVICE_LOOKUP_ATTEMPTS = 3;
 const PERSISTED_DEVICE_LOOKUP_RETRY_MS = 250;
+const REMOTE_LOG_PREVIEW_CHARS = 4000;
+function previewForRemoteLog(value, maxChars = REMOTE_LOG_PREVIEW_CHARS) {
+    try {
+        const text = JSON.stringify(value);
+        return text.length > maxChars ? `${text.slice(0, maxChars)}…[truncated ${text.length - maxChars} chars]` : text;
+    }
+    catch {
+        return '[unserializable]';
+    }
+}
 export function getRemoteDeviceConfigPath() {
     return path.join(os.homedir(), '.desktop-commander-device', 'device.json');
 }
@@ -370,7 +380,7 @@
             console.debug('[DEBUG] Ignoring tool call for different device');
             return;
         }
-        console.log(`🔧 Received tool call ${call_id}: ${tool_name} ${JSON.stringify(tool_args)} metadata: ${JSON.stringify(metadata)}`);
+        console.log(`🔧 Received tool call ${call_id}: ${tool_name} ${previewForRemoteLog(tool_args, 2000)} metadata: ${previewForRemoteLog(metadata, 1000)}`);
         // LOCAL claim first — this is the authoritative guard against executing
         // a call twice. During the transition both transports deliver every call
         // to THIS SAME PROCESS, so an in-memory check is sufficient and, unlike
@@ -421,7 +431,7 @@
                 // Execute other tools using desktop integration
                 result = await this.desktop.callClientTool(tool_name, tool_args, metadata);
             }
-            console.log(`✅ Tool call ${tool_name} completed:\r\n ${JSON.stringify(result)}`);
+            console.log(`✅ Tool call ${tool_name} completed: ${previewForRemoteLog(result)}`);
             // The result write itself notifies the server (a DB trigger).
             await this.remoteChannel.updateCallResult(call_id, 'completed', result);
         }
